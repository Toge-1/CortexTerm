# CortexTerm 项目面经

> 使用口径：Python 版 CortexTerm 为个人独立开发项目，个人负责整体架构、记忆系统、终端 TUI 与 Docker 评测链路。以下内容以当前仓库源码和作者提供的 `SWE-bench 44/50（88%）` 为基础；正式投递前应归档对应的官方评测报告。

## 1. 项目简介（简历可用）

独立开发 Python 终端 Coding Agent CortexTerm，以多步 Agent Loop 统一编排代码检索、编辑、命令执行等 29 个内置工具及 MCP 扩展能力，并实现事件驱动 TUI、用户/项目/本地三级记忆、会话恢复与上下文压缩。搭建隔离的 SWE-bench 补丁生成和官方 Docker 判分链路，在 50 个评测样本中解决 44 个，完成率 88%（作者提供，正式使用前需附官方报告）。

## 2. 简历 bullet

- **可扩展任务编排：** 针对单轮问答无法完成“定位—修改—验证—再修复”的连续研发任务，独立设计多步 Agent Loop，将模型输出、工具调用、权限上下文和结果回填收敛到统一状态推进机制，并加入最大步数、空响应续跑、超时与工具错误兜底，使 29 个内置工具及后续扩展无需侵入核心调度逻辑。
- **交互执行解耦：** 针对模型请求和命令执行耗时导致终端冻结的问题，独立实现事件驱动 TUI，以主线程管理输入、模式路由和渲染，后台线程执行 Agent turn，通过线程事件承接权限审批，并以节流重绘和可见窗口渲染控制长对话开销，使工具进度、审批与滚动浏览可在长任务中持续响应。
- **分层知识管理：** 针对用户偏好、项目共识与本地私有信息传播范围不同的问题，独立实现 user/project/local 三级记忆和显式 `remember` 写入工具，采用结构化 JSON 与可读 Markdown 双格式持久化，并在 token 预算内分层注入系统上下文，使跨会话知识具备清晰的共享与隔离边界。
- **会话可靠性治理：** 针对长时间编码任务中的退出恢复与上下文溢出问题，独立实现会话索引、定时自动保存、临时文件加原子替换，以及中英文 token 估算和阈值触发的上下文压缩；同时分离模型消息与可见 transcript，保证折叠展示不会破坏工具调用和结果之间的协议因果链。
- **开放协议扩展：** 针对内置工具难以覆盖外部生态的问题，实现 stdio MCP 客户端，通过 JSON-RPC 完成工具、资源与提示词发现，并动态包装为统一工具定义；以请求 id、等待队列、超时、stderr 采集和进程回收管理调用生命周期，使外部能力可接入统一注册表并复用 Agent Loop。
- **可复现基准评测：** 针对补丁生成、局部测试和官方 resolved 容易混淆的问题，独立搭建基于基线提交与 Git worktree 的 SWE-bench 生成链路，加入测试文件修改拦截、无补丁重试和编译反馈，再将 predictions 交给官方 Docker harness 依据 `FAIL_TO_PASS` 与 `PASS_TO_PASS` 判分；作者记录为 44/50（88%），正式使用前保留汇总与逐实例报告。

## 3. 面试问题

### 主题一：项目定位与核心架构

#### Q1（高频）：为什么要做 CortexTerm？它解决的核心问题是什么？

**第一人称口播（STAR）：** 我做 CortexTerm 的出发点不是再包一层聊天接口，而是把模型变成能在真实仓库里持续推进任务的终端 Agent。实际编码任务通常要先理解目录和符号，再读文件、修改、运行测试，并依据失败继续处理，单次问答无法维持这条执行链。我的任务是独立搭出一个最小但完整的系统，所以我把模型适配、Agent Loop、统一工具注册、权限、TUI、记忆和评测拆成明确模块。结果是用户可以在终端给出目标，由 Agent 连续调用 29 个内置工具或 MCP 能力完成操作，同时系统保留审批、会话恢复和基准评测边界。这个项目最能证明的是我把 LLM 能力落成工程系统的能力，而不只是调用一次 API。

**追问 1：它和普通命令行聊天机器人有什么本质区别？**

**第一人称口播（STAR）：** 我区分两者时会看系统有没有可执行闭环。普通 CLI 聊天机器人一般接收文本并打印文本，模型即使建议命令，也没有统一工具协议、真实执行结果和后续修复步骤。CortexTerm 的一轮任务会让模型返回结构化工具调用，注册表在工作区和权限上下文中执行，再把结果原样回填给模型，模型可以据此决定继续读文件、改代码还是跑测试。我还加入最大步骤、异常退出和空响应恢复，避免循环失控。最终判断不是“回复看起来合理”，而是仓库是否形成补丁、检查是否执行、官方评测是否 resolved，因此它是执行型 Agent，而不是文本外壳。

**追问 2：为什么选择 Python 和终端形态？**

**第一人称口播（STAR）：** 我的目标是先验证 Agent 的核心执行能力和评测闭环，Python 标准库足以完成 HTTP、子进程、线程、文件系统和 JSON-RPC，能把依赖控制得很小，也方便与代码评测脚本直接集成。终端形态贴近开发者现有工作流，能够自然访问仓库、Git、测试命令和环境变量，不必先设计复杂的网页后端。我在实现中仍把 TUI、Agent Loop 和工具层分开，因此终端只是当前交互适配层，不会绑死核心运行时。结果是项目可以快速迭代底层能力，并在 Windows 和普通管道模式下运行；若以后增加 GUI，也可以复用模型、工具、权限和会话模块。

#### Q2（高频）：Agent Loop 是怎样工作的？

**第一人称口播（STAR）：** 我把 Agent Loop 设计成以消息序列为状态的有限步骤循环。进入一轮任务后，模型根据系统提示词、用户消息和历史工具结果生成下一步；如果是最终回答就收口，如果是工具调用，就由注册表完成工具查找、输入校验和执行，再把工具结果追加到消息序列后继续请求模型。工程上我没有只处理理想路径，还增加了最大步骤、网络错误、超时、未知异常、空响应和 thinking 阶段中断的恢复逻辑，并通过回调把助手文本、进度、工具开始和结果投影给 TUI。这样既保留核心循环的单一职责，也让不同界面和评测脚本复用同一执行入口。

**追问 1：为什么需要最大步骤，达到上限后怎么办？**

**第一人称口播（STAR）：** 工具型 Agent 可能因为定位错误、工具失败或模型反复确认而进入循环，如果没有上限，一次请求会持续消耗 token、时间和外部资源。我在 Agent Loop 中把 `max_steps` 作为明确预算，交互模式和评测模式都可以按任务调整；每一步只允许模型产生一个可处理状态，执行结果继续进入消息历史。达到上限时系统停止继续调用模型，并输出可诊断的收口信息，而不是假装任务完成。验证时我会构造始终请求同一工具的 mock 模型，确认调用次数严格受限，同时检查已产生的消息和工具结果仍可保存，方便用户下一轮继续或调整目标。

**追问 2：空响应、pause turn 或 max tokens 怎么处理？**

**第一人称口播（STAR）：** 我把这些情况视为不同故障，而不是统一吞掉。普通空响应最多做有限次数提示续写；如果诊断显示模型在 thinking 阶段因为 `pause_turn` 或 `max_tokens` 停止，则用更具体的恢复提示要求继续下一步，并限制恢复次数。若工具已经执行过，提示还会强调基于工具结果收口，避免模型只停在过程描述。超过重试预算后，系统生成包含 stop reason、块类型和工具错误数量的诊断文本并结束。这样用户能区分模型暂时中断、真正空回答和工具失败，评测脚本也能把 agent error 与 no patch 分开记录。

#### Q3（高频）：你如何设计统一工具体系？新增工具要改哪里？

**第一人称口播（STAR）：** 为避免 Agent Loop 认识每个具体工具，我定义了统一工具描述和注册表：工具声明名称、用途、输入 schema、校验器和执行函数，执行时再收到可信的工作目录与权限对象。Agent Loop 只通过名称查找并调用注册表，不关心它是文件读取、代码搜索、命令执行还是 MCP 远端能力。当前默认注册表静态装配 29 个内置工具，并在启动时追加动态发现的 MCP 工具。新增普通工具时只需实现定义并在注册入口注册；若工具涉及路径、命令或编辑，还要显式调用相应权限检查。结果是核心调度保持稳定，扩展能力的改动面集中且可以单独测试。

**追问 1：`ToolContext` 为什么不能直接让模型传入？**

**第一人称口播（STAR）：** `ToolContext` 里包含真实工作目录和权限管理器，这些属于运行时可信状态。如果把它作为模型可填写参数，模型可以伪造工作区路径或绕过用户授权，所以我把它和工具业务输入分离：模型只能生成 schema 允许的普通参数，注册表执行时由宿主注入 context。具体工具需要访问文件时，用 context 解析并检查目标路径；执行命令或修改文件时，再触发对应审批。这个设计把不可信模型输出与可信宿主能力隔开。验证上我会传入越界路径和伪造字段，确认工具只采用宿主 context，并检查拒绝结果能作为 tool result 返回给模型继续处理。

**追问 2：29 个工具是不是越多越好？**

**第一人称口播（STAR）：** 工具数量不是目标，关键是每个工具的语义是否清晰、是否存在重叠，以及模型能否稳定选择。我保留文件读写、搜索、代码导航、Git、测试、调试、Web、任务管理等覆盖研发闭环的能力，并用统一 schema 描述，但不会因为数量好看继续堆工具。工具过多会增加系统提示词长度和选择歧义，相似编辑工具也可能让模型反复试错。后续我会按真实调用日志统计选择正确率、失败率和替代关系，对低使用或高混淆工具做合并，并考虑按任务阶段动态暴露工具。当前“29”只能证明覆盖面和注册能力，不能单独证明 Agent 质量。

### 主题二：TUI 与并发状态

#### Q4（高频）：为什么 TUI 要使用后台线程？线程边界如何划分？

**第一人称口播（STAR）：** 模型请求、网络等待和命令执行都可能很慢，如果放在终端主循环里，用户无法滚动、取消或处理权限审批。我把主线程限定为终端输入、模式路由和实际绘制的唯一拥有者，每次 Agent turn 在后台守护线程执行。后台线程通过回调更新 transcript、工具卡片和进度状态，只提出重绘请求；真正的刷新由主线程事件循环统一 `flush`。权限请求使用线程事件让 Agent 线程等待用户选择，而主线程继续渲染审批界面。结果是长任务期间界面仍能处理输入和状态变化，同时避免多个线程同时写终端导致光标和画面破坏。

**追问 1：共享状态会不会有竞态？你怎么处理？**

**第一人称口播（STAR）：** 会有风险，因为后台回调会更新 transcript 和工具状态，而主线程同时读取这些状态渲染。我当前通过明确的所有权减少竞态：终端 I/O 与最终绘制只在主线程，后台线程不直接操作终端；高频回调只修改有限状态并触发节流请求，完成结果由事件循环集中收割。审批结果通过 `threading.Event` 做同步，渲染器内部也有锁保护 pending 状态。这个方案适合当前单 Agent turn，但我不会把它描述成完全无竞态。若扩展并行子任务，我会改成线程安全事件队列，让后台只发送不可变事件，主线程归并状态，并补充压力与顺序一致性测试。

**追问 2：为什么不用 asyncio？**

**第一人称口播（STAR）：** 选择线程主要基于现有依赖和阻塞接口：标准输入读取、同步模型适配器、subprocess 以及许多工具都是同步实现，直接强行改成 asyncio 会在边界处大量使用 executor，复杂度并不会消失。我的任务是先建立清晰的交互与执行隔离，所以用一个后台线程配合主事件循环能较小改动地解决界面冻结和审批等待。当前系统并不追求大量并发 Agent 请求，线程模型足够。若未来需要并行工具、多个会话或流式网络复用，我会重新评估 async runtime，但会先用吞吐、取消传播和状态复杂度做对比，而不是仅因技术流行迁移。

#### Q5（高频）：为什么模型 messages 和界面 transcript 必须分离？

**第一人称口播（STAR）：** 两者服务的对象不同。模型 messages 必须保留系统提示、用户消息、结构化工具调用和对应结果，下一次模型请求依赖这条因果链；界面 transcript 面向人，需要工具卡片、折叠、摘要、滚动位置和可见窗口。如果共用一份数据，为了显示而截断工具输出可能让模型丢失证据，为了协议完整而展示全部数据又会让 TUI 难以使用。我因此让后台 Agent 更新完整 messages，并通过回调把可读投影写入 transcript；会话保存同时保留必要状态。结果是 UI 可以折叠与窗口化，而模型仍基于完整工具链继续推理，二者通过明确桥接而不是隐式共享。

**追问 1：两份状态如何避免不一致？**

**第一人称口播（STAR）：** 我把一致性定义为“语义事件可追踪”，而不是要求两份结构逐字段相同。用户提交时同时追加用户消息和用户 transcript；工具开始与结果由同一 Agent 回调产生，分别进入模型消息和展示卡片；最终回答同样从统一回调写入展示。后台执行完成后，主事件循环只在拿到完整结果时替换 messages，避免使用半成品。恢复测试要核对用户输入、工具调用、工具结果和最终回答的顺序，并允许 transcript 对长输出做摘要。若发现漏事件，我会增加稳定 event id 做映射，而不会让 UI 直接读取模型协议块。

**追问 2：为什么 transcript 可以截断而工具结果不能随便截断？**

**第一人称口播（STAR）：** transcript 截断只影响用户当前看到的预览，完整内容仍可通过展开或保存查看；模型工具结果则可能包含错误堆栈、命中代码或测试失败，后续决策依赖这些细节。如果在写入 messages 前按 UI 宽度截断，模型可能因为缺少最后几行错误或关键路径而做出错误修复。我因此把显示摘要放在 `tool_display` 和 transcript 渲染层，Agent 消息层保留协议需要的结果。真正需要控制上下文时，由 ContextManager 做有语义的压缩并记录事件，而不是让 UI 截断副作用污染模型。验证时我会构造关键错误位于长输出尾部的案例，确认模型上下文仍能读取。

#### Q6（中高频）：长对话 TUI 如何控制渲染开销和闪烁？

**第一人称口播（STAR）：** 长任务会产生大量消息和工具输出，如果每次进度回调都全量格式化并重画 transcript，终端容易闪烁，耗时也随历史长度增长。我做了两层控制：第一层是节流渲染器，把短时间内的多次重绘请求合并，实际绘制由主事件循环按最小间隔刷新；第二层是窗口化 transcript，根据终端宽度、可见行数和滚动偏移，只渲染当前窗口，并缓存条目的行级结果。工具输出先生成简短卡片，用户需要时再展开。这个设计让渲染工作主要与可见区域相关。具体 P95 帧耗时还没有形成正式数据，后续要用千级和万级消息压测验证。

**追问 1：终端宽字符和滚动偏移有什么坑？**

**第一人称口播（STAR）：** 终端中的中文、emoji、ANSI 转义和普通 ASCII 的显示宽度不同，直接用 Python 字符串长度会算错换行；如果总行数和实际渲染采用两套算法，滚动最大偏移就会漂移，表现为到底后仍有空白或无法看到最后一行。我在 transcript 侧把可见窗口、条目行数和命中区域统一基于终端宽度的物理行计算，并让鼠标区域复用相同范围。验证时会组合中文、长代码、颜色、折叠工具卡和窗口 resize，检查顶部、底部、PageUp/PageDown 与滚轮行为。这个细节体现的是状态与渲染规则要共享，而不是靠坐标补丁修问题。

**追问 2：16ms 最小间隔意味着 60 FPS 吗？**

**第一人称口播（STAR）：** 不能直接这样宣称。16ms 只是节流器允许两次实际重绘之间的最小间隔，用于合并高频请求，它给出了理论上的刷新上限，但实际帧率还受终端尺寸、Markdown 格式化、输出量、操作系统控制台和主循环读取影响。我会把它表述为“以约 16ms 的最小重绘间隔抑制重复刷新”，不会写成稳定 60 FPS。若要量化，我会在固定终端尺寸下记录 request 次数、真正 render 次数、单帧耗时和输入延迟，并分别测试流式文本、工具大输出与快速滚动，最后报告 P50/P95 而非理论值。

### 主题三：记忆、会话与 Skill

#### Q7（高频）：为什么设计三级记忆？各层怎么划分？

**第一人称口播（STAR）：** 我遇到的矛盾是长期知识既要复用又要控制传播范围。比如用户偏好的表达方式适合跨项目，项目架构决策应跟随仓库协作，本机路径或未公开信息只适合本地。如果全部写进一个全局文件，会污染其他项目并带来泄漏风险；全部放项目里又无法复用个人偏好。我因此划分 user、project、local 三个 scope，分别映射到用户目录、项目共享目录和项目本地目录，并要求 `remember` 写入时显式选择。系统同时保存 JSON 便于结构化管理、生成 Markdown 便于人工审阅，启动时按预算注入相关内容。结果是记忆生命周期与共享范围可以被解释和验证。

**追问 1：三层出现冲突时谁优先？**

**第一人称口播（STAR）：** 当前实现按 local、project、user 的顺序拼接上下文，主要意图是先放最贴近当前工作区的信息，但它并没有实现基于 key 的严格覆盖或冲突消解，所以我不会声称系统已经具备成熟优先级语义。遇到同一事实矛盾时，模型可能同时看到多条记录，需要依赖内容和时间判断。下一步我会给记忆增加稳定主题键、更新时间和冲突规则，例如 local 覆盖 project，project 覆盖 user，同时保留来源供模型解释。验证会构造三层同名事实，检查最终提示词顺序、选择结果和人工可追溯性，再决定是否需要去重或显式冲突提示。

**追问 2：为什么同时保存 JSON 和 Markdown？**

**第一人称口播（STAR）：** JSON 和 Markdown解决的是两个不同需求。结构化 JSON 适合稳定保存 id、scope、category、时间和标签，也方便程序做增删改查；Markdown 适合用户直接在仓库中阅读、审查和版本控制。如果只用 Markdown，解析自由文本会脆弱；如果只用 JSON，人工查看和手工修订成本高。我在保存时由同一内存对象生成两种表示，使它们表达同一组条目。这个双写也带来一致性风险，所以后续应采用原子写并把 JSON 作为规范源，Markdown 作为派生视图。测试要覆盖异常中断、手工编辑和重复加载，避免一条记忆被解析两次。

#### Q8（高频）：会话恢复如何设计，怎么避免保存出空文件？

**第一人称口播（STAR）：** Coding Agent 的任务可能持续很久，进程退出、终端关闭或机器重启后，如果消息和工具轨迹丢失，用户很难继续。我设计了会话正文和元数据索引，记录工作区、消息、transcript、创建与更新时间，并用自动保存管理器在状态变脏且达到间隔后落盘。为避免直接覆盖过程中崩溃造成 0 字节文件，我先写临时文件，再通过原子替换更新目标；空会话或无意义内容不会污染最近会话列表。恢复时既可以按 id，也可以取当前工作区最新会话。结果是大多数正常和意外退出场景都能续接，但磁盘损坏与跨版本迁移仍需额外机制。

**追问 1：自动保存为什么不是每次状态变化都写？**

**第一人称口播（STAR）：** 每次 token、进度或工具卡变化都写磁盘，会产生大量小写入，并放大索引和正文不一致的机会。我采用 dirty 标记加最小时间间隔：状态变化只标记需要保存，事件循环周期检查是否到达保存时机；程序正常结束时再强制保存。这样把高频内存变化合并为较低频持久化，同时仍把可能丢失的窗口控制在可解释范围。当前默认间隔是 30 秒，它是工程折中而非可靠性承诺。验证时我会模拟短任务、长工具执行、连续输入和强制退出，记录最后一次可恢复点，并根据实际写入成本调整间隔。

**追问 2：索引损坏或正文不存在怎么办？**

**第一人称口播（STAR）：** 我不会把索引视为唯一事实源，因为它只是加速会话列表和最近会话选择。读取时需要容忍 JSON 解析失败、缺失文件和字段不完整，对单个坏会话做隔离，不能让整个 CLI 无法启动。更稳妥的恢复策略是先尝试索引，异常时扫描会话正文重建元数据；删除时同步清理索引，但也要处理文件已经不存在的幂等情况。当前代码已有从会话文件提取元数据的路径，后续应补自动重建与版本字段。验证会人工截断索引、删除正文、制造临时文件残留，确认可用会话仍能列出并恢复。

#### Q9（中高频）：上下文压缩是怎么触发和工作的？

**第一人称口播（STAR）：** 长会话会持续累积系统提示、用户消息、工具调用和大段结果，直接等到模型报 context overflow 会让任务突然失败。我实现了 ContextManager，按模型窗口估算每条消息的 token，区分中英文与代码字符，并在使用率接近阈值时于 Agent turn 开始前触发压缩。压缩保留系统消息和最近交互，把较旧内容转成摘要，同时记录压缩前后 token、消息数和摘要，通过回调显示在 TUI。这样用户知道上下文发生过变化，Agent 也能继续工作。它解决的是容量治理，摘要质量仍要通过长任务成功率验证，不能只看 token 下降。

**追问 1：为什么压缩要保留最近消息？**

**第一人称口播（STAR）：** 最近消息通常包含当前任务目标、刚执行的工具结果和尚未解决的错误，若全部摘要，最容易丢失精确路径、参数和失败原因。我在压缩时保留一定数量的尾部消息，把更早的背景聚合为摘要，从而在降低 token 的同时维持局部因果链。尤其是工具调用与结果不能随意拆开，否则模型会看到没有来源的输出或找不到对应调用。验证时我会构造多轮编辑任务，在压缩点前后检查最后一次测试失败、当前文件和待办是否仍被模型正确引用；若失败，就调整保留窗口或把关键状态结构化，而不是单纯提高阈值。

**追问 2：token 估算不精确会有什么影响？**

**第一人称口播（STAR）：** 本地估算不可能完全等同于服务端 tokenizer，中文、代码、JSON 和不同模型的切分都会造成偏差。低估会让系统来不及压缩而触发 API 超限，高估则会过早摘要并损失上下文。我采用中英文不同的字符比例，并把触发阈值留出安全余量，但这仍是启发式方案。工程上我会记录估算值与 API 返回 usage 的差异，按模型校准系数，并在服务端给出 token 计数时优先使用真实值。评测应同时看超限率、压缩次数和任务完成率，不能只优化估算误差本身。

#### Q10（高频）：CortexTerm 的 Skill 是怎样加载的？references 会自动进入上下文吗？

**第一人称口播（STAR）：** 当前 Skill 采用渐进加载思路。启动时系统扫描项目和用户的 `.cortexterm/skills`，同时兼容 `.claude/skills`，只把 Skill 名称、描述、路径和来源写进系统提示词；当用户点名或任务语义命中时，模型调用 `load_skill`，这时对应 `SKILL.md` 全文才作为工具结果进入上下文。因此 references、scripts 和 assets 不会自动全部加载，这能控制 token，但当前实现也存在明确缺口：安装命令只复制 `SKILL.md`，加载结果没有稳定的包根信息，也没有专门读取引用资源的工具。我会把它称为基础版渐进加载，而不是完整插件系统。

**追问 1：模型怎么知道 reference 在哪个目录？**

**第一人称口播（STAR）：** 只依赖 `SKILL.md` 中的相对路径还不够，运行时必须知道这个路径相对哪个可信根解析。当前 `load_skill` 会返回 `SKILL.md` 的绝对路径，因此模型理论上能推导同级目录，但如果安装时没有复制 references，或者 Skill 使用跨目录共享资源，这条路径仍然失效。更稳妥的设计是把 `skill_dir` 和 `plugin_root` 作为加载结果的一部分，再提供 `read_skill_resource(skill, relative_path)`，由宿主解析并限制目标必须位于允许的包根内。这样模型不需要猜目录，也不能用 `..` 读取任意文件，引用链可以被测试和审计。

**追问 2：为什么不在启动时加载所有 Skill 和 reference？**

**第一人称口播（STAR）：** 启动时全部加载会迅速占满系统提示词，而且大量 Skill 与当前任务无关，会增加模型注意力竞争和错误路由。我的目标是先用短摘要完成发现，再按任务加载一个 Skill 的规则，只有规则明确要求时才读取具体 reference；脚本和工具应通过执行或注册提供能力，而不是作为文本全部塞给模型。这样上下文成本与实际使用的 Skill 数量相关。验证时要比较全量加载和渐进加载的 prompt token、首请求延迟、Skill 选择正确率与任务成功率，并测试同名 Skill 的优先级和资源越界，避免只凭设计直觉判断。

### 主题四：MCP 与权限边界

#### Q11（高频）：MCP 工具从启动到真正执行经过哪些步骤？

**第一人称口播（STAR）：** 启动时我先读取 MCP Server 配置并创建 stdio 子进程，完成初始化后请求 `tools/list`，也尝试发现 resources 和 prompts。每个远端工具描述会被转换成统一的 ToolDefinition，名称加入 server 前缀避免冲突，输入 schema 保留下来。模型选择该工具后，注册表调用包装闭包，客户端发送带递增 id 的 JSON-RPC `tools/call`；stdout 读取线程解析 newline JSON 或 Content-Length 帧，再按 id 把响应放进对应等待队列。请求有超时，stderr 保留最近诊断，关闭时会唤醒未完成请求并回收进程。结果是 Agent Loop 无需感知远端协议细节，外部能力也能沿用统一的执行结果和错误处理方式。

**追问 1：为什么闭包要绑定 client 和工具名？**

**第一人称口播（STAR）：** 动态注册通常发生在循环中，如果闭包直接引用循环变量，Python 的晚绑定会让所有工具在执行时都指向最后一个 client 或最后一个工具名，这是很隐蔽的错误。我在包装函数的默认参数中固定当前 `_client` 和 `_descriptor_name`，确保每个 ToolDefinition 保留创建时的路由目标。验证不能只看 tools/list 数量，而要启动一个暴露多个不同返回值的测试 Server，逐个调用并核对它们到达正确方法；还要覆盖两个 Server 使用同名工具的情况，确认前缀和闭包绑定都生效。这是动态扩展中典型的语言语义与协议路由交叉问题。

**追问 2：MCP 子进程突然退出时怎么避免请求永远等待？**

**第一人称口播（STAR）：** 每个请求会创建一个有界等待队列并登记到 pending 表，正常响应按 id 取出并唤醒调用方。如果 stdout 读取循环发现 EOF，说明子进程已经退出，我会获取退出码，构造错误消息并遍历所有 pending 队列，把错误放进去后清空表；主动 close 也做同样的唤醒。单个请求本身还有 timeout，超时后从 pending 删除并附带 stderr 诊断。这样无论进程异常退出、协议无响应还是主动关闭，调用方都能在有限时间得到失败结果，Agent 可以选择降级或向用户报告，而不会永久挂住整个 turn。

#### Q12（高频）：权限系统保护了什么？MCP 启动过滤等于完整沙箱吗？

**第一人称口播（STAR）：** 权限管理器主要保护本地工具的路径访问、命令执行和文件编辑。工具执行时由宿主注入工作区根和权限对象，读取越界路径、危险命令或修改文件会触发对应检查，交互模式可让用户选择单次或持久授权。MCP 启动前的 command 和 args 校验解决的是减少明显危险启动字符串，并且 Popen 使用参数数组而非 shell；它并不自动审查远端 MCP 工具内部会读哪些文件、访问哪些网络或执行什么命令。当前 MCP 包装闭包也没有使用 ToolContext 做二次授权，所以我会明确说这是启动过滤和本地工具权限，而不是进程级沙箱。

**追问 1：`allow_once` 真的是只允许一次吗？**

**第一人称口播（STAR）：** 从当前代码语义看，`allow_once` 和 `deny_once` 保存在管理器实例的集合中，生命周期更接近当前进程或会话，而不是严格消费一次后删除。因此我不会在面试中把它说成单次调用授权，否则行为和命名不一致。修复方向有两种：如果产品意图是“本次会话”，就重命名为 session allow/deny；如果意图真是一次，就在匹配后立即消费授权记录，并对并发工具调用做原子保护。验证需要连续发起两次相同路径或命令请求，确认第二次是否重新询问，同时覆盖 turn 开始和结束时的状态清理。

**追问 2：如果要给 MCP 增加真正安全边界，你会怎么做？**

**第一人称口播（STAR）：** 我会先按能力而不是进程名称建模权限，例如文件读写根目录、命令执行、网络域名和密钥访问，再让每个 MCP Server 在配置中声明能力，启动前展示给用户。运行层面可以把 Server 放进容器、受限账户或操作系统沙箱，挂载只读工作区，并按需要暴露代理网络；对 `tools/call` 记录 server、方法、参数摘要、耗时和结果。ToolContext 只能约束宿主包装层，无法自动控制远端进程内部行为，所以必须结合进程隔离。验证会使用恶意测试 Server 尝试越界读写与联网，确认配置、运行时和审计日志三层都能发现或阻止。

### 主题五：SWE-bench 与工程验证

#### Q13（高频）：SWE-bench 补丁生成链路是怎样设计的？

**第一人称口播（STAR）：** 每个评测样本包含问题描述、仓库和基线提交。我的生成器先准备仓库缓存，确认 `base_commit` 存在，再为实例创建 detached worktree，保证不同样本互不污染。随后用同一 Agent Loop、工具注册表和权限上下文执行修复，只把问题描述与公开元数据给模型。任务结束后收集 Git 状态和 `model_patch`，清理临时文件，拒绝修改测试路径的补丁；若没有补丁会有限重试，Python 源码有语法错误则把编译输出反馈给 Agent 再修。最终逐行写 predictions，并支持 resume 跳过已有实例。这个阶段目标是可靠地产出可审计补丁，不直接宣布官方 resolved。

**追问 1：为什么每个实例要用 Git worktree？**

**第一人称口播（STAR）：** SWE-bench 的每个问题都绑定不同仓库状态或基线提交，如果复用同一个工作目录，上一个 Agent 的修改、未跟踪文件或依赖状态会污染下一个实例，导致结果不可复现。worktree 允许共享 Git 对象但给每个实例独立工作目录，我按 case id 生成安全路径并以 detached 方式落在精确 base commit 上；结束后默认移除，也可以为调试保留。创建前检查已有目录，force 行为显式控制，避免误删不相关路径。验证时会并排运行两个实例，核对 HEAD、git status、补丁和清理结果，确保互不串扰。

**追问 2：为什么生成阶段不能应用 `test_patch`？**

**第一人称口播（STAR）：** `test_patch` 往往包含为该问题新增或修改的验证逻辑，如果在 Agent 生成补丁前应用，模型可以直接读取隐藏测试的断言和边界，结果就不再代表根据 issue 独立修复。我在生成结果中明确记录 `test_patch_status` 为未应用，并通过补丁审计拒绝 Agent 修改测试文件。生成阶段最多运行仓库已有测试或公开可推导的检查；真正判分时由官方 harness 在隔离 Docker 环境应用模型补丁和 benchmark 测试补丁。这个边界能把“产生答案”和“裁判答案”分开，防止数据泄漏，也使不同 Agent 的 predictions 可由同一裁判复测。

#### Q14（高频）：你说 44/50，怎样证明这个数字可信？

**第一人称口播（STAR）：** 我会先说明评测单位是 50 个 SWE-bench 实例，其中官方判定 resolved 44 个，所以算术结果是 88%。这个数字目前来自项目作者提供，我不会用普通 pytest 通过数或生成了补丁的数量替代它。完整证据应包括固定的数据集文件与哈希、每个实例的 `instance_id`、模型和预算配置、生成的 predictions、官方 harness 的 run id、汇总 JSON，以及 50 份实例报告或能逐项对账的 report。只有 `FAIL_TO_PASS` 和 `PASS_TO_PASS` 都符合官方规则才算 resolved。正式写入公开简历前，我会把这些材料归档并用脚本核对分母、resolved 列表和 error 列表。

**追问 1：本地测试通过为什么不等于 SWE-bench resolved？**

**第一人称口播（STAR）：** 本地测试可能只覆盖仓库现有用例，也可能因依赖缺失而根本没跑到关键路径；SWE-bench 的官方环境会构建特定镜像，应用模型补丁和 benchmark 的测试补丁，并分别检查原来失败的用例是否转为通过，以及原来通过的回归用例是否保持通过。生成器内部的 `py_compile` 只排除语法错误，可选 test command 也只是早期反馈，它们都不是最终裁判。我在结果模型中把 compile status、local test status、patch status 和 official resolved 分开，避免一个绿色信号覆盖所有阶段。对外只引用官方报告中的实例级结论。

**追问 2：50 个样本能代表真实能力吗？**

**第一人称口播（STAR）：** 50 个样本能提供一个有明确任务单位的阶段性结果，但不足以代表完整 SWE-bench 分布，更不能直接外推到所有仓库和语言。可信度取决于样本如何选择、是否存在污染、模型配置、预算和失败类型。如果样本经过人工筛选或来自特定仓库，88% 可能明显偏离更大集合。我会在简历中写清“50 个样本中 44 个 resolved”，不写榜单排名；后续扩展到固定公开 split，报告置信区间、仓库分布、patch rate、error rate 和成本，并保留失败案例。这样面试官能判断适用范围，而不是被一个百分比误导。

#### Q15（高频）：项目目前最大的不足是什么？下一步先改什么？

**第一人称口播（STAR）：** 我会坦率分三类。第一，Skill 目前只有摘要发现和 `SKILL.md` 按需加载，安装只复制单文件，references 与 scripts 的包语义不完整；第二，MCP 的启动参数过滤不等于远端能力沙箱，ToolContext 也未在远端闭包中形成细粒度审批；第三，当前回归曾观察到 212 个通过、6 个失败，失败涉及真实 API、记忆格式和工作区路径语义，需要分类修复后再建立稳定基线。下一步我优先完成 Skill 整目录安装和受限资源读取，因为它直接影响用户刚需且边界清晰，然后修测试基线，再增强 MCP 能力声明与隔离。每项都用可失败的测试验收。

**追问 1：为什么先改 Skill，而不是继续加工具？**

**第一人称口播（STAR）：** 当前 29 个内置工具已经覆盖主要开发动作，继续增加相似工具的边际收益不明确，反而会扩大提示词和选择空间。Skill 是把复杂工作流、参考资料和脚本组织成可复用能力的入口，但现状会在安装时丢失 references，导致看似装好了、真正执行时找不到资料，这是明确的端到端断点。我会先把安装改为复制完整目录，加载结果携带包根，再增加受限相对路径读取和 YAML frontmatter 解析，并用带共享引用的真实 Skill 做测试。完成后再根据使用日志决定哪些能力应是工具、哪些应是 Skill，而不是继续堆数量。

**追问 2：你怎么判断架构不是过度设计？**

**第一人称口播（STAR）：** 我用是否解决已出现的具体矛盾来判断。Agent Loop 是多步工具任务必需；messages 与 transcript 分离来自展示截断和模型协议的冲突；后台线程来自真实模型请求阻塞；三级记忆来自传播范围不同；worktree 来自评测实例污染风险。每个模块都能对应失败现象和单独验证，而不是为了套设计模式。对于尚未出现的并行调度、分布式队列或复杂数据库，我没有提前引入。后续如果某层只有一个实现且没有独立边界，我会考虑合并；如果压测或故障注入证明当前抽象无收益，也会删除。架构应由可复现问题和维护成本共同驱动。

## 4. 源码证据索引

| 主题 | 关键路径与内部符号 | 对应正文位置 |
| --- | --- | --- |
| 启动与依赖装配 | `cortexterm/main.py:130 main`、`:220 create_default_tool_registry`、`:238 MemoryManager` | 项目简介、Q1 |
| Agent Loop | `cortexterm/agent_loop.py:82 run_agent_turn`、空响应与 thinking 恢复辅助函数 | bullet 1、Q2 |
| 模型适配 | `cortexterm/anthropic_adapter.py:166 AnthropicModelAdapter`、`cortexterm/api_retry.py` | Q2 |
| 工具抽象 | `cortexterm/tooling.py:105 ToolContext`、`:115 ToolDefinition`、`:123 ToolRegistry` | bullet 1、Q3 |
| 内置工具装配 | `cortexterm/tools/__init__.py:35 create_default_tool_registry`；当前静态枚举 29 个 | 项目简介、Q3 |
| TUI 总入口 | `cortexterm/tty_app.py:180 run_tty_app`、文件头的状态边界说明 | bullet 2、Q4、Q5 |
| Agent 后台执行 | `cortexterm/tui/agent_turn.py:17 start_agent_turn`、`:83 run_agent_background` | Q4 |
| 事件循环 | `cortexterm/tui/event_loop.py:19 run_terminal_event_loop`、`:73 _harvest_completed_agent_turn` | Q4、Q5 |
| 节流与窗口化渲染 | `cortexterm/tui/rendering.py:10 ThrottledRenderer`、`cortexterm/tui/transcript.py:429 render_transcript` | bullet 2、Q6 |
| TUI 状态 | `cortexterm/tui/state.py:23 TtyAppArgs`、`:65 ScreenState` | Q4、Q5 |
| 三级记忆 | `cortexterm/memory.py:28 MemoryScope`、`:191 MemoryManager`、`:346 get_relevant_context` | bullet 3、Q7 |
| 记忆工具 | `cortexterm/tools/remember.py` | bullet 3、Q7 |
| 会话保存 | `cortexterm/session.py:279 _atomic_write_text`、`:290 save_session`、`:440 AutosaveManager` | bullet 4、Q8 |
| 上下文压缩 | `cortexterm/context_manager.py:132 ContextManager`、`cortexterm/agent_loop.py:104` 附近的压缩触发 | bullet 4、Q9 |
| Skill 发现与安装 | `cortexterm/skills.py:38 _skill_roots`、`:72 discover_skills`、`:88 load_skill`、`:110 install_skill` | Q10、Q15 |
| Skill 工具和提示词 | `cortexterm/tools/load_skill.py`、`cortexterm/prompt.py:89 build_system_prompt` | Q10 |
| MCP 客户端 | `cortexterm/mcp.py:193 StdioMcpClient`、`:401 request`、`:443 call_tool` | bullet 5、Q11 |
| MCP 动态注册 | `cortexterm/mcp.py:500 create_mcp_backed_tools` | Q11 |
| 本地权限 | `cortexterm/permissions.py:160 PermissionManager`、`:222 ensure_path_access`、`:269 ensure_command`、`:322 ensure_edit` | Q12 |
| SWE-bench 生成 | `evals/run_swebench_harness.py:182 create_worktree`、`:365 invalid_patch_reason`、`:443 run_py_compile`、`:680 run_code_edit_case` | bullet 6、Q13 |
| 官方 Docker 判分 | `../../swebench-official-run/run_extra18_contaminated_eval.py` 调用 `swebench.harness.run_evaluation.main` | bullet 6、Q14 |
| TUI 架构说明 | `docs/tui-architecture.md` | Q4–Q6 |
| 完整运行时说明 | `docs/agent-runtime-flow.md` | Q1–Q3、Q11–Q12 |

## 5. 可核验证据与待补项

### 已有或可直接从源码核验

- Python 包入口为 `cortexterm = cortexterm.main:main`，要求 Python 3.11 及以上。
- 默认注册表当前可静态枚举 29 个内置工具，MCP 工具按运行时配置追加。
- TUI 使用主线程事件循环、后台 Agent 线程、线程事件审批和节流重绘。
- 记忆系统定义 user/project/local 三层，并提供 `remember` 工具。
- session 使用临时文件加原子替换，并有自动保存和恢复入口。
- Skill 发现支持 `.cortexterm/skills` 与 `.claude/skills` 的项目/用户目录，正文按需加载。
- SWE-bench 生成脚本使用 `base_commit`、Git worktree、补丁审计、无补丁重试和编译反馈。
- 官方评测脚本直接调用 `swebench.harness.run_evaluation.main`，运行在 Docker harness 语义下。

### 需要正式投递前补齐

- `44/50（88%）` 对应的数据集版本、样本清单与哈希。
- 模型名称、推理参数、每实例最大步骤、超时和失败重试配置。
- 完整 predictions 文件、官方汇总 JSON、resolved/error 列表与逐实例 `report.json`。
- 29 个工具的版本快照；后续代码变动后应重新枚举。
- TUI 输入延迟、长 transcript 渲染耗时、内存与上下文压缩收益的压测数据。
- 当前测试中的 6 个失败应分类为产品缺陷、环境依赖或测试预期问题，修复后再给出通过率。

## 6. 交给 `/great-resume` 的项目事实摘要

- **项目名称与方向：** CortexTerm；AI Agent / 开发者工具 / Python 工程方向。
- **个人职责边界：** 独立开发 Python 版，负责整体架构、Agent 运行时、终端 TUI、三级记忆、会话与上下文治理、MCP 接入和 Docker 评测链路。
- **关键技术动作：** 统一多步 Agent Loop；29 个内置工具与动态 MCP 注册；主线程 TUI 加后台 Agent 线程；messages/transcript 分离；三级记忆；原子会话保存；上下文压缩；Skill 渐进加载；worktree 隔离生成和官方 Docker 判分。
- **可核验证据：** 以上模块均有对应源码；工具数量可运行时枚举；官方评测调用入口存在。
- **候选表述：** 优先使用本文六条简历 bullet；项目简介可直接用于一页简历。
- **待补指标：** 44/50 的完整官方报告、TUI 性能数据、修复后的全量测试结果。

## 7. 交给 `/interview` 的高风险 Claim 清单

| Claim 类型 | 高风险表述 | 风险原因 | 面试准备 |
| --- | --- | --- | --- |
| Ownership | “独立开发整个 Python 版 CortexTerm” | 需要能解释全部关键模块与代码演进，且不能把上游或参考实现冒认为原创 | 准备提交历史、模块边界、个人实现清单和参考来源说明 |
| Metric | “SWE-bench 44/50、88%” | 当前仓库未发现可直接对账的完整 50 例官方报告集合 | 归档数据集、predictions、汇总 JSON 和 50 份实例结果 |
| Architecture | “设计核心 Agent 架构” | 会被追问循环状态、停止条件、工具回填、错误恢复和上下文压缩 | 熟练讲 Q2、Q3、Q9，并能现场追源码 |
| Architecture | “实现高响应 TUI” | “高响应”若无延迟数据容易被质疑 | 用线程边界和窗口化机制表述；补 P50/P95 压测 |
| Architecture | “实现完整 Skill 系统” | 当前安装只复制 `SKILL.md`，references/scripts 还未形成完整包能力 | 对外说“本地 Skill 发现与按需加载”，主动说明演进项 |
| Architecture | “MCP 有权限沙箱” | 启动过滤不等于远端进程行为隔离 | 清楚区分本地权限、MCP 参数校验和真正进程沙箱 |
| Result | “测试通过率很高” | 当前前序检查仍有 6 个失败，历史 README 数字不能代替当前结果 | 修复并锁定测试环境后再给出数字；现阶段不写此 Claim |
| Result | “达到生产级稳定性” | 缺少长期使用、故障注入和性能数据 | 改为可验证机制描述，并说明待测计划 |
